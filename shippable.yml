language: c

compiler:
  - gcc
  - clang

matrix:
  - exclude:
    - compiler: gcc
  - allow_failures:
    - compiler: clang
    

build:
  pre_ci:
    - echo 'true'

  ci:
    - ./configure && make && make install
    - ./hello
    - echo $NONEXISTANT

  post_ci:
    - echo "true"
