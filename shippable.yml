language: c

compiler:
  - gcc
  - clang

matrix:
  exclude:
    - compiler: gcc
  allow_failures:
    - compiler: clang
    


# matrix:
#   include:
#     - python: 3.2
#   exclude:
#     - python: 2.7
#   allow_failures:
#     - compiler: "gcc"

build:
  pre_ci:
    - echo 'true'
    - ls -la ../idontexist

  ci:
    - ./configure && make && make install
    - ./hello

  post_ci:
    - echo "true"
